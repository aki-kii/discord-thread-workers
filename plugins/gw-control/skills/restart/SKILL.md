---
name: restart
description: Discord スレッド中継の GW セッションを立て直す。「GW を再起動」「中継を入れ直して」、プラグインのコードを直した後などに使う。
---

`${CLAUDE_PLUGIN_ROOT}/bin/gw-restart` を実行し、**出力をそのまま利用者に見せてください。**

worker には触れません。GW だけが入れ替わります。
実行時プラグインは起動のたびに読み込まれるので、コードを直した後はこれで反映されます。
