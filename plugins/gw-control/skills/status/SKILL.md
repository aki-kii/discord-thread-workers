---
name: status
description: Discord スレッド中継の GW と worker セッションの一覧を出す。「GW の状態」「worker 一覧」「今どのスレッドが動いてる」などのときに使う。
---

`${CLAUDE_PLUGIN_ROOT}/bin/gw-status` を実行し、**出力をそのまま利用者に見せてください。**

整形し直したり、要約したりしないこと。表示はスクリプト側で整えてあります。
